<header>
  <h1 class="page-title">All Your Feeds</h1>
</header>

<ul>
  <% feeds.forEach( function(feed) { %>
  <li>
    <a href="#/feeds/<%=feed.id%>"><%= feed.escape("name") %></a>
    <button class="remove-feed" data-id=<%=feed.id%>>Remove Feed</button>
  </li>
  <% }); %>

</ul>
